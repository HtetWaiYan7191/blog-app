<div class="new-comment-container">
    <h1>Add a new comment</h1>
<%= form_with model: @comment, url: user_post_comments_path do |c| %>
    <%= c.label :text%>
    <%= c.text_area :text, cols: 30, rows:1%><br>
    <%= c.submit "Submit", class:"new-post-btn" %>
<% end %>
</div>

